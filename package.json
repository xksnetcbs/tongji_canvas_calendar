{
  "name": "canvas-caldav-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Sync Canvas assignments to a CalDAV calendar",
  "type": "module",
  "scripts": {
    "start": "node canvas_calendar_sync.mjs",
    "sync": "node canvas_calendar_sync.mjs",
    "check": "node --check canvas_calendar_sync.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
